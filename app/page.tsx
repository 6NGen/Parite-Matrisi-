import Matrix from '@/components/Matrix';

// Statik önbelleğe alınıp bayat HTML sunulmasın diye sayfayı dinamik tut.
export const dynamic = 'force-dynamic';

export default function Page() {
  return <Matrix />;
}
